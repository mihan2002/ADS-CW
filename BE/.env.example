# Server
PORT=3000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database (MySQL connection string)
DATABASE_URL=mysql://root@localhost:3306/cw1

# JWT (required)
JWT_SECRET=replace-with-long-random-secret
JWT_REFRESH_SECRET=replace-with-long-random-refresh-secret
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# University registration restriction (comma-separated domains)
UNIVERSITY_EMAIL_DOMAINS=university.edu

# API keys (required for /api/client/*)
API_KEY_PEPPER=replace-with-long-random-pepper

# Email (Gmail SMTP via nodemailer)
EMAIL_USER=mihanfernando11@gmail.com
app_password=asib oqrs vncn cuzy

